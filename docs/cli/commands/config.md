# `slab config`

Read and modify CLI configuration.

## Usage

```bash
slab config
slab config get <key>
slab config set <key> <value>
slab config reset [--yes]
```

## Examples

```bash
slab config
slab config set api_url https://api.goskilla.com
slab config get api_url
slab config reset --yes
```
